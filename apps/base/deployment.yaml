apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app
spec:
  replicas: 1 # Default for dev/test
  selector:
    matchLabels:
      app: web-app
  template:
    metadata:
      labels:
        app: web-app
    spec:
      containers:
      - name: nginx
        image: nginx:1.25 # Base version
        ports:
        - containerPort: 80
