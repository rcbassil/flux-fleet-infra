apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-values
  namespace: flux-system
data:
  defaults.yaml: |
    replicaCount: 2
    automountServiceAccountToken: true
    service:
      type: ClusterIP
