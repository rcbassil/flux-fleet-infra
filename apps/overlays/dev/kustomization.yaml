apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
namePrefix: dev-
commonLabels:
  env: development
patches:
  - target:
      kind: HelmRelease
      name: my-nginx
    patch: |
      - op: replace
        path: /spec/values/replicaCount
        value: 1
